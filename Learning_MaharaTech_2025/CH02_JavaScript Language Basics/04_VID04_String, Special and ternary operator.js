// String operators (+)
var firstname = "Ali";
var lastname = "kareem";
var fullname = firstname + lastname;
console.log(fullname);

// or can we write with another way :
var firstname = "Ali",
lastname = "kareem",
fullname = firstname + lastname,
console.log(fullname)

// Special operators (comma operators (,) , typeof  , Ternary )
// Ternary (condition?val1:val2)
// vla1 if condition is true , val2 if condition is false 
<script>
    var temp = 120 ;
        newvar = (temp>100) ? `red` : `blue`;
        // the value of newvar is red
        temp = 20 ;
        newvar = (temp>100) ? `red` : `blue`;
        // the value of newvar is blue
</script>

